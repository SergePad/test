name: test
on:
  workflow_dispatch:
   inputs:
      project_name:
        type: string
        description: "name of the project"
      environment:
        type: string
        description: "environment"
      website:
        type: string
      mail:
        type: string
      
jobs:
  test:
    runs-on: ubuntu-latest
    name: test
    steps:
     - name: test
       run: echo ${{ inputs.project_name}}.${{ inputs.environment }}
     - name: test2
       run: echo &{{ inputs.website }}/${{ inputs.mail}}

