name: test
on:
  workflow_dispatch:
   inputs:
      project_name:
        type: string
        description: "name of the project"
jobs:
  test:
    runs-on: ubuntu-latest
    name: test
    steps:
     - name: test
       run: echo ${{ inputs.my_variable }}

